Document principles such as:

Assign roles to groups where possible.
Follow least privilege.
Use built-in roles unless a custom role is justified.
Prefer PIM for privileged access.
Assign permissions at the smallest appropriate scope.